/**
 * This enum is used when an object needs to choose a direction to move on the
 * game map.
 * @author William Randle
 * @version 1.0
 */
public enum Direction {
	
	North(), 
	East(), 
	South(), 
	West();
}
